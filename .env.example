# NEXMART — 현재 First Build는 환경변수 없이 동작합니다 (Shared Demo Repository).
# 아래는 READY 단계 연결 시 사용할 예약 키입니다. 값을 넣어도 현재 코드는 읽지 않습니다.

# Supabase (DB · Auth · RLS) — READY
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=

# AI API — Executive Briefing 자연어 설명 1개부터 연결 — READY
AI_API_KEY=

# Analytics (GA4 / PostHog) — Event Adapter — READY
NEXT_PUBLIC_ANALYTICS_KEY=
